import { StyleSheet } from 'react-native';

const LogoutStyles = StyleSheet.create({
  container: {
    width: 50,
    height: 50,
    padding: 8,
  },
  logoutIcon: {
    width: '100%',
    height: '100%',
  },
});

export default LogoutStyles;
